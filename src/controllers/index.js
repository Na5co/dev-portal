const {
  authController,
  userController,
  bankingController,
  devController,
  adminController,
} = require('../controllers');

module.exports = {
  authController,
  userController,
  bankingController,
  devController,
  adminController,
};
